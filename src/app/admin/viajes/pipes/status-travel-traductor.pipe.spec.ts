import { StatusTravelTraductorPipe } from './status-travel-traductor.pipe';

describe('StatusTravelTraductorPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusTravelTraductorPipe();
    expect(pipe).toBeTruthy();
  });
});
