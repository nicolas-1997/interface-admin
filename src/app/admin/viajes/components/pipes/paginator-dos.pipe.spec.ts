import { PaginatorDosPipe } from './paginator-dos.pipe';

describe('PaginatorDosPipe', () => {
  it('create an instance', () => {
    const pipe = new PaginatorDosPipe();
    expect(pipe).toBeTruthy();
  });
});
