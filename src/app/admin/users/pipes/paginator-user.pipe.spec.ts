import { PaginatorUserPipe } from './paginator-user.pipe';

describe('PaginatorUserPipe', () => {
  it('create an instance', () => {
    const pipe = new PaginatorUserPipe();
    expect(pipe).toBeTruthy();
  });
});
